# Content Whale Frontend

## First Run

To start the development server:

1. Make sure your backend server is running(view testimonials).
   - Example: `php artisan serve` for Laravel backend.

2. Run the frontend development server:

```bash
npm install
npm run dev
